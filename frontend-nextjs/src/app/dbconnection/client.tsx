import { TDbConnectionOutRequestDto } from "@/core/application/dtos/dbconnection.dto";

export default function DbConnectionClient({
  initialData,
}: {
  initialData: TDbConnectionOutRequestDto[];
}) {
  return <div>DB Connection Page</div>;
}
